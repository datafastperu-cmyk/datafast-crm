// Seeds are handled by migration 1700000010000-SeedInitialData.
// This file exists only to satisfy the npm run seed:run script.
console.log('Seeds already applied via migration SeedInitialData1700000010000.');
process.exit(0);
