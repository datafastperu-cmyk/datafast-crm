# CRM ISP DATAFAST — Configuración por defecto
# Este archivo es cargado por el instalador

NODE_VERSION=20
PG_VERSION=16
INSTALL_DIR=/opt/datafast
APP_USER=datafast
APP_PORT_BACKEND=4000
APP_PORT_FRONTEND=3000
MIN_RAM_MB=1500
MIN_DISK_GB=5

# OpenVPN — Infraestructura VPN para equipos ISP
VPN_PORT=1194
VPN_PROTO=tcp
VPN_NETWORK=10.8.0.0
VPN_NETMASK=255.255.255.0
VPN_MAX_CLIENTS=100
